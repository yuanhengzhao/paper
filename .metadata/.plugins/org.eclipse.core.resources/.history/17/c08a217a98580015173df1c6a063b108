require 'spec_helper'

describe Forms::PreTaskSafetyPlansController do
  it_behaves_like "a form controller" do
   let(:model) { Forms::PreTaskSafetyPlan }
    let(:image_fields) { [ :signature ] }
    let(:pre_model_name) { "" }
  end
end