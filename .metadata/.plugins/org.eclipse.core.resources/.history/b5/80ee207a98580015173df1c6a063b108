require 'spec_helper'

describe Forms::NetPromoterScoresController do
  it_behaves_like "a form controller" do
    let(:model) { Forms::NetPromoterScore }
    let(:image_fields) { [] }
    let(:pre_model_name) { "" }
  end
end